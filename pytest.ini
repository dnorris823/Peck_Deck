[pytest]
testpaths = backend/tests
# These flag pre-existing app style (inferred deps / path params) that will
# change in Litestar 3.0 and a short test-secret; not test failures.
filterwarnings =
    ignore:.*Inferred dependency field.*
    ignore:.*deprecated inferred style.*
    ignore:.*HMAC key.*
