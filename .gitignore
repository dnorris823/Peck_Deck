# Python
.venv/
venv/
__pycache__/
*.pyc
.env

# Local dev database (SQLite)
*.db
*.sqlite3

# Database backups (contain real sighting images and user records)
backups/
*.dump

# Node / Vite build cache
node_modules/
frontend/.vite/
frontend/dist/

# OS
.DS_Store

# Hardware-test stand-in ML assets (not the real model)
machine_learning/stand_in_smoketest_224_uint8.tflite
smoke_capture.jpg

# Model weights — fetched, not vendored. `python scripts/fetch_models.py`
machine_learning/*.tflite
machine_learning/*.pth

# Ground-truth photos cached by scripts/validate_tier1.py. Third-party images
# under their own licences — re-fetched on demand, never vendored.
.validation_photos/
