fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
# ML — install the CUDA build of torch before running. The RTX 5080 is Blackwell
# (compute capability 12.0 / sm_120), which first appears in the cu128 wheels;
# the cu124 build carries no sm_120 kernels and cannot run on this card:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.3
torchvision>=0.18
timm>=1.0
Pillow>=10.0
